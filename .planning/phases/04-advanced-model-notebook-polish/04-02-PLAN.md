---
phase: 04-advanced-model-notebook-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - notebook_gradient_boost.ipynb
  - notebook_logistic.ipynb
  - notebook_random_forest.ipynb
autonomous: true
requirements:
  - EVAL-04

must_haves:
  truths:
    - "notebook_gradient_boost.ipynb has a title/intro markdown cell as the first cell of the notebook"
    - "notebook_gradient_boost.ipynb has a results interpretation markdown cell as the last cell of the notebook"
    - "notebook_logistic.ipynb has a title/intro markdown cell as the first cell of the notebook"
    - "notebook_logistic.ipynb has a results interpretation markdown cell as the last cell of the notebook"
    - "notebook_random_forest.ipynb has a title/intro markdown cell as the first cell of the notebook"
    - "notebook_random_forest.ipynb has a results interpretation markdown cell as the last cell of the notebook"
    - "All three notebooks still run end-to-end without error after markdown additions"
    - "Each intro cell mentions the specific model used and why it was chosen"
    - "Each results cell references the 49.6% naive baseline for comparison"
  artifacts:
    - path: "notebook_gradient_boost.ipynb"
      provides: "GBM notebook with bookend markdown documentation"
      contains: "HistGradientBoostingClassifier"
    - path: "notebook_logistic.ipynb"
      provides: "LR notebook with bookend markdown documentation"
      contains: "LogisticRegression"
    - path: "notebook_random_forest.ipynb"
      provides: "RF notebook with bookend markdown documentation"
      contains: "RandomForestClassifier"
  key_links:
    - from: "intro cell (first cell)"
      to: "## Setup section header cell"
      via: "cell ordering in notebook JSON cells array"
      pattern: "cells\\[0\\].*cell_type.*markdown"
    - from: "last code cell (predict_match demo)"
      to: "results interpretation cell (last cell)"
      via: "cell ordering in notebook JSON cells array"
      pattern: "cells\\[-1\\].*cell_type.*markdown"
---

<objective>
Add two markdown documentation cells to each of the three model notebooks (notebook_logistic.ipynb, notebook_random_forest.ipynb, notebook_gradient_boost.ipynb) to satisfy EVAL-04.

Purpose: Make all three notebooks submission-ready. Each notebook bookends its existing content with (1) a title/intro cell explaining the dataset, problem framing, and model rationale, and (2) a results interpretation cell explaining the metrics, draw class performance, and comparison to the 49.6% naive baseline.

Output: Three updated notebooks, each with exactly two new markdown cells inserted as the first cell and the last cell. Section header cells and all code cells are unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-advanced-model-notebook-polish/04-CONTEXT.md
@.planning/phases/04-advanced-model-notebook-polish/04-01-SUMMARY.md

<interfaces>
<!-- Key decisions from 04-CONTEXT.md governing documentation content -->

D-05: Add exactly two new markdown cells per notebook:
  1. Title/intro cell — inserted as cell index 0 (before ## Setup)
  2. Results interpretation cell — appended as the final cell (after predict_match demo cell)

D-06: Section headers (## Setup, ## Load Data, ## Model Training, etc.) remain unchanged — no prose mid-notebook.

D-07: Intro cell briefly states the model used and why it was chosen.
  - LR: "Logistic Regression provides a linear probabilistic baseline. It is fast, interpretable, and serves as the performance floor for ensemble models."
  - RF: "Random forests handle non-linear interactions well and are robust to overfitting with balanced class weights."
  - GBM: "Gradient boosting iteratively corrects residuals and can learn subtle patterns in form data, targeting 65-70% accuracy."

Required content for intro cell (3-4 sentences covering):
  - Dataset: Brazilian Série A championship matches (9,165 matches, 2003-2025), train/test split at 2022/2023 boundary
  - Problem framing: Predict W/D/L match outcome from rolling 5-match form features
  - Model approach: why this specific model was chosen (D-07 rationale)

Required content for results interpretation cell (covering):
  - What the metrics mean (precision/recall/F1 per class)
  - Draw class performance (lowest recall due to class imbalance; class_weight='balanced' helps but draws remain hardest to predict)
  - Comparison to naive baseline (49.6% "always HomeWin" — per CLAUDE.md, the correct figure is 49.6% not 46%)
  - Primary metric is macro-F1 (not raw accuracy), because class_weight='balanced' trades accuracy for recall on minority classes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert title/intro and results interpretation cells into all three model notebooks</name>
  <files>notebook_gradient_boost.ipynb, notebook_logistic.ipynb, notebook_random_forest.ipynb</files>
  <read_first>
    - notebook_logistic.ipynb — read full notebook JSON to know exact current cell structure (first cell is ## Setup markdown, last cell is predict_match demo code cell); insert intro before cell 0, append results after last cell
    - notebook_random_forest.ipynb — same structure check; confirm first and last cells before inserting
    - notebook_gradient_boost.ipynb — created in Plan 01; confirm first cell is ## Setup, last cell is predict_match demo; insert intro before cell 0, append results after last cell
    - .planning/phases/04-advanced-model-notebook-polish/04-CONTEXT.md — D-05, D-06, D-07 (exact content rules for each cell)
    - CLAUDE.md — baseline figure is 49.6% (not 46%); use this in results cells
  </read_first>
  <action>
    For each of the three notebooks, modify the cells array in the notebook JSON as follows:

    OPERATION: Prepend one markdown cell (new index 0) and append one markdown cell (new last cell). All existing cells shift index by 1; their content is unchanged.

    --- notebook_logistic.ipynb intro cell content ---
    Title: "# Brazilian League Match Predictor — Logistic Regression"
    Body (3-4 sentences): This notebook predicts the outcome (Home Win / Draw / Away Win) of Brazilian Série A championship matches using rolling 5-match form features derived from 9,165 historical matches (2003-2025). The dataset is split temporally: training on seasons through 2022, evaluation on 2023-2025. Logistic Regression is used as the baseline model — it provides a fast, interpretable linear probabilistic classifier and establishes the performance floor against which ensemble models are compared. Features include rolling goals scored/conceded, win/draw/loss form, season win percentages, and points accumulated over the last five matches per team.

    --- notebook_logistic.ipynb results cell content ---
    Title: "## Results Interpretation"
    Body: The classification_report above shows precision, recall, and F1 for each of the three outcome classes. The naive baseline — predicting "Home Win" for every match — achieves 49.6% accuracy; this model trades raw accuracy for balanced recall across all three classes via class_weight='balanced'. The Draw class consistently shows the lowest recall, reflecting the genuine difficulty of predicting draws (approximately 26% of matches) from form data alone. The primary evaluation metric is macro-F1, which weights all three classes equally and gives a fuller picture of multi-class performance than accuracy alone.

    --- notebook_random_forest.ipynb intro cell content ---
    Title: "# Brazilian League Match Predictor — Random Forest"
    Body (3-4 sentences): This notebook predicts the outcome (Home Win / Draw / Away Win) of Brazilian Série A championship matches using rolling 5-match form features derived from 9,165 historical matches (2003-2025). The dataset is split temporally: training on seasons through 2022, evaluation on 2023-2025. Random Forest is chosen because it handles non-linear interactions between form features effectively and is robust to overfitting when configured with min_samples_leaf and balanced class weights. Features include rolling goals scored/conceded, win/draw/loss form, season win percentages, and points accumulated over the last five matches per team.

    --- notebook_random_forest.ipynb results cell content ---
    Title: "## Results Interpretation"
    Body: The classification_report above shows precision, recall, and F1 for each of the three outcome classes. The naive baseline — predicting "Home Win" for every match — achieves 49.6% accuracy; Random Forest trades raw accuracy for balanced recall across all three classes via class_weight='balanced'. The Draw class consistently shows the lowest recall, reflecting the genuine difficulty of predicting draws from form data alone. The primary evaluation metric is macro-F1, which weights all three classes equally. Compare this notebook's macro-F1 with the Logistic Regression and Gradient Boosting notebooks to see how ensemble complexity affects multi-class performance.

    --- notebook_gradient_boost.ipynb intro cell content ---
    Title: "# Brazilian League Match Predictor — Gradient Boosting"
    Body (3-4 sentences): This notebook predicts the outcome (Home Win / Draw / Away Win) of Brazilian Série A championship matches using rolling 5-match form features derived from 9,165 historical matches (2003-2025). The dataset is split temporally: training on seasons through 2022, evaluation on 2023-2025. HistGradientBoostingClassifier is chosen because gradient boosting iteratively corrects residuals and can learn subtle non-linear patterns in form data; HistGBT is preferred over classic GBM because it natively supports class_weight='balanced' and trains significantly faster on this dataset. Features include rolling goals scored/conceded, win/draw/loss form, season win percentages, and points accumulated over the last five matches per team.

    --- notebook_gradient_boost.ipynb results cell content ---
    Title: "## Results Interpretation"
    Body: The classification_report above shows precision, recall, and F1 for each of the three outcome classes. The naive baseline — predicting "Home Win" for every match — achieves 49.6% accuracy; Gradient Boosting targets the 65-70% accuracy range while maintaining balanced recall via class_weight='balanced'. The Draw class typically shows the lowest recall, as draws are the hardest outcome to predict from form data alone (approximately 26% of matches). The primary evaluation metric is macro-F1; the accuracy figure should be read alongside recall per class rather than in isolation. Compare this notebook's metrics with notebook_logistic.ipynb and notebook_random_forest.ipynb to assess whether the additional model complexity delivers measurable gains.

    Implementation note: Edit the notebook JSON directly (the .ipynb file is JSON). For each notebook:
    1. Load JSON from the file
    2. Prepend the intro markdown cell to the cells array (insert at index 0)
    3. Append the results interpretation markdown cell to the cells array
    4. Write back as valid JSON

    Markdown cell JSON structure:
    {"cell_type": "markdown", "id": "<unique uuid4>", "metadata": {}, "source": ["line1\n", "line2\n", ...]}
  </action>
  <verify>
    <automated>cd /home/theo/AI/Projeto_2_AI && source .venv/bin/activate && pytest --nbmake notebook_logistic.ipynb notebook_random_forest.ipynb notebook_gradient_boost.ipynb -x -v 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All three notebooks run end-to-end via pytest --nbmake without error (exit 0)
    - notebook_logistic.ipynb: first cell (index 0) is markdown type containing '# Brazilian League Match Predictor — Logistic Regression'
    - notebook_logistic.ipynb: last cell is markdown type containing '## Results Interpretation'
    - notebook_logistic.ipynb: last cell contains '49.6%' baseline reference
    - notebook_random_forest.ipynb: first cell (index 0) is markdown type containing '# Brazilian League Match Predictor — Random Forest'
    - notebook_random_forest.ipynb: last cell is markdown type containing '## Results Interpretation'
    - notebook_random_forest.ipynb: last cell contains '49.6%' baseline reference
    - notebook_gradient_boost.ipynb: first cell (index 0) is markdown type containing '# Brazilian League Match Predictor — Gradient Boosting'
    - notebook_gradient_boost.ipynb: last cell is markdown type containing '## Results Interpretation'
    - notebook_gradient_boost.ipynb: last cell contains '49.6%' baseline reference
    - Each intro cell mentions the model name and a rationale (per D-07): 'interpretable' or 'linear' for LR; 'non-linear interactions' for RF; 'iteratively corrects residuals' or 'HistGBT' for GBM
    - Section header cells (## Setup, ## Load Data, etc.) are unchanged in all three notebooks
    - No code cells were modified (only new markdown cells were added)
  </acceptance_criteria>
  <done>All three model notebooks have a title/intro markdown cell as their first cell and a results interpretation markdown cell as their last cell, and all three still execute end-to-end via nbmake without error. EVAL-04 is satisfied: data source, feature engineering choices, model selection rationale, and results interpretation are all present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner-authored text → notebook cells | Documentation text is static, planner-controlled; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04B-01 | Tampering | notebook JSON editing | mitigate | Load/write JSON programmatically; validate notebook still runs via nbmake before marking done |
| T-04B-02 | Denial of Service | Malformed JSON breaks nbmake | mitigate | Validate JSON structure (cells array, required cell_type fields) before writing; nbmake test confirms correctness |
| T-04B-SC | Tampering | No new package installs in this plan | accept | Documentation-only changes; no new dependencies |
</threat_model>

<verification>
Run end-to-end test for all three notebooks:
  cd /home/theo/AI/Projeto_2_AI && source .venv/bin/activate && pytest --nbmake notebook_logistic.ipynb notebook_random_forest.ipynb notebook_gradient_boost.ipynb -x -v

Spot-check documentation: confirm each notebook's first cell contains the model name title and last cell contains "Results Interpretation" and references the 49.6% baseline.
</verification>

<success_criteria>
All three model notebooks have clean bookend documentation cells (intro + results interpretation), run end-to-end without error, and satisfy EVAL-04: each notebook explains the dataset source, feature engineering choices, model selection rationale, and results interpretation including comparison to the 49.6% naive baseline and discussion of Draw class performance.
</success_criteria>

<output>
Create `.planning/phases/04-advanced-model-notebook-polish/04-02-SUMMARY.md` when done.
</output>
