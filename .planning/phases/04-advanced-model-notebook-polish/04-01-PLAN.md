---
phase: 04-advanced-model-notebook-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebook_gradient_boost.ipynb
autonomous: true
requirements:
  - MODEL-03
  - EVAL-04

must_haves:
  truths:
    - "notebook_gradient_boost.ipynb runs end-to-end without error"
    - "HistGradientBoostingClassifier with class_weight='balanced' is the fitted model"
    - "TimeSeriesSplit(n_splits=5) cross_val_score is reported for accuracy and macro-F1"
    - "classification_report shows non-zero recall for HomeWin, Draw, and AwayWin"
    - "Confusion matrix heatmap is produced"
    - "Baseline comparison cell prints naive_acc=0.4816 and model accuracy and macro-F1"
    - "predict_match('Flamengo', 'Palmeiras') returns a W/D/L label without error"
  artifacts:
    - path: "notebook_gradient_boost.ipynb"
      provides: "GBM model notebook — 8-section structure identical to Phase 3 notebooks"
      contains: "HistGradientBoostingClassifier"
  key_links:
    - from: "notebook_gradient_boost.ipynb"
      to: "dados/feature_matrix_train.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet.*feature_matrix_train"
    - from: "notebook_gradient_boost.ipynb"
      to: "HistGradientBoostingClassifier"
      via: "sklearn.ensemble import"
      pattern: "HistGradientBoostingClassifier"
    - from: "predict_match function"
      to: "hgb (fitted model)"
      via: "hgb.predict(vector)"
      pattern: "hgb\\.predict"
---

<objective>
Create notebook_gradient_boost.ipynb — the GBM model notebook for the Brazilian League Match Predictor.

Purpose: Deliver MODEL-03 (HistGradientBoostingClassifier with class_weight='balanced', targeting 65-70% accuracy on the 2023-2025 test set) using the same 8-section structure as the Phase 3 notebooks so the project has three comparable model notebooks.

Output: A fully-executed notebook_gradient_boost.ipynb with trained model, CV scores, classification_report, confusion matrix heatmap, baseline comparison, and predict_match function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-advanced-model-notebook-polish/04-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Key types and patterns extracted from Phase 3 notebooks. Executor replicates these exactly. -->

From notebook_logistic.ipynb / notebook_random_forest.ipynb (established patterns):

Section structure (8 markdown header cells in order):
  ## Setup
  ## Load Data
  ## Feature Selection
  ## Model Training
  ## Cross-Validation
  ## Evaluation
  ## Baseline Comparison
  ## predict_match

Setup cell pattern:
  import os
  os.environ['MPLBACKEND'] = 'agg'
  os.environ['MPLCONFIGDIR'] = '/tmp/matplotlib-config'
  import difflib, numpy, pandas, seaborn, matplotlib.pyplot
  from sklearn.ensemble import HistGradientBoostingClassifier
  from sklearn.model_selection import TimeSeriesSplit, cross_val_score
  from sklearn.metrics import classification_report, confusion_matrix, accuracy_score, f1_score

Feature Selection pattern:
  NON_FEATURE_COLS = ['id', 'date', 'season', 'round', 'home_team', 'away_team',
                      'home_score', 'away_score', 'home_state', 'away_state', 'result']
  FEATURE_COLS = [c for c in train.columns if c not in NON_FEATURE_COLS]
  X_train, y_train, X_test, y_test = train[FEATURE_COLS], train['result'], test[FEATURE_COLS], test['result']

Evaluation pattern:
  labels = ['HomeWin', 'Draw', 'AwayWin']
  y_pred = hgb.predict(X_test)
  print(classification_report(y_test, y_pred, labels=labels, target_names=labels))
  cm = confusion_matrix(y_test, y_pred, labels=labels)
  sns.heatmap(cm, annot=True, fmt='d', xticklabels=labels, yticklabels=labels, cmap='Blues')
  plt.title('Gradient Boosting — Confusion Matrix')

Baseline comparison pattern:
  naive_acc = (y_test == 'HomeWin').mean()
  model_acc = accuracy_score(y_test, y_pred)
  model_f1 = f1_score(y_test, y_pred, average='macro')

predict_match pattern (model-agnostic — substitute 'hgb' for 'lr'/'rf'):
  HOME_FEAT_COLS = [c for c in FEATURE_COLS if c.startswith('home_')]
  AWAY_FEAT_COLS = [c for c in FEATURE_COLS if c.startswith('away_')]
  combined = pd.concat([train, test]).sort_values('date').reset_index(drop=True)
  home_last = combined.groupby('home_team')[HOME_FEAT_COLS].last()
  away_last = combined.groupby('away_team')[AWAY_FEAT_COLS].last()
  VALID_TEAMS = sorted(combined['home_team'].unique().tolist())
  def predict_match(home_team, away_team) -> str: ...
  return hgb.predict(vector)[0]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build notebook_gradient_boost.ipynb with 8-section structure and trained HGBC model</name>
  <files>notebook_gradient_boost.ipynb</files>
  <read_first>
    - notebook_logistic.ipynb — exact cell structure, import pattern, evaluation cell, baseline comparison cell, predict_match cells (cells 0-17); replicate verbatim except for model class and variable name
    - notebook_random_forest.ipynb — confirm section header wording matches; use as secondary reference
    - .planning/phases/04-advanced-model-notebook-polish/04-CONTEXT.md — D-01 through D-07 decisions (HGBC params, structure, predict_match pattern)
    - CLAUDE.md — "Critical Gotchas" section (temporal leakage rules, draw class collapse, baseline = 49.6%)
  </read_first>
  <action>
    Create notebook_gradient_boost.ipynb as a new Jupyter notebook (.ipynb JSON) with 18 cells in the following order. The model variable name throughout is 'hgb'.

    Cell 0 (markdown): ## Setup
    Cell 1 (code): os.environ setup (MPLBACKEND=agg, MPLCONFIGDIR=/tmp/matplotlib-config), then imports: os, difflib, numpy as np, pandas as pd, seaborn as sns, matplotlib.pyplot as plt, then from sklearn.ensemble import HistGradientBoostingClassifier, from sklearn.model_selection import TimeSeriesSplit cross_val_score, from sklearn.metrics import classification_report confusion_matrix accuracy_score f1_score.
    Cell 2 (markdown): ## Load Data
    Cell 3 (code): train = pd.read_parquet('dados/feature_matrix_train.parquet'), test = pd.read_parquet('dados/feature_matrix_test.parquet'), print train.shape and test.shape.
    Cell 4 (markdown): ## Feature Selection
    Cell 5 (code): Define NON_FEATURE_COLS list with all 11 non-feature columns (id, date, season, round, home_team, away_team, home_score, away_score, home_state, away_state, result). FEATURE_COLS = [c for c in train.columns if c not in NON_FEATURE_COLS]. Assign X_train, y_train, X_test, y_test. Print len(FEATURE_COLS).
    Cell 6 (markdown): ## Model Training
    Cell 7 (code): Instantiate and fit the model — per D-01 and D-03:
      hgb = HistGradientBoostingClassifier(
          learning_rate=0.05,   # smaller rate + more trees: better generalization
          max_iter=300,
          max_depth=5,
          class_weight='balanced',
          random_state=42,
      )
      hgb.fit(X_train, y_train)
      print('Model trained.')
    Cell 8 (markdown): ## Cross-Validation
    Cell 9 (code): Per D-04 — tss = TimeSeriesSplit(n_splits=5). Run cross_val_score with scoring='accuracy' and scoring='f1_macro'. Print CV accuracy mean +/- std and CV macro-F1 mean +/- std.
    Cell 10 (markdown): ## Evaluation
    Cell 11 (code): y_pred = hgb.predict(X_test). Print classification_report with labels=['HomeWin', 'Draw', 'AwayWin'] and target_names=['HomeWin', 'Draw', 'AwayWin'].
    Cell 12 (code): labels = ['HomeWin', 'Draw', 'AwayWin']. confusion_matrix, then plt.figure(figsize=(6,5)), sns.heatmap(cm, annot=True, fmt='d', xticklabels=labels, yticklabels=labels, cmap='Blues'), title='Gradient Boosting — Confusion Matrix', plt.tight_layout(), plt.show().
    Cell 13 (markdown): ## Baseline Comparison
    Cell 14 (code): naive_acc = (y_test == 'HomeWin').mean(). model_acc = accuracy_score(y_test, y_pred). model_f1 = f1_score(y_test, y_pred, average='macro'). Print all three values. Print note that class_weight=balanced trades raw accuracy for Draw/AwayWin recall and that primary metric is macro-F1.
    Cell 15 (markdown): ## predict_match
    Cell 16 (code): HOME_FEAT_COLS / AWAY_FEAT_COLS split, combined = pd.concat([train, test]).sort_values('date').reset_index(drop=True), home_last and away_last groupby lookups, VALID_TEAMS list. Define predict_match(home_team, away_team) function using difflib.get_close_matches for ValueError hint (same logic as notebook_logistic.ipynb cell 16, with 'hgb' instead of 'lr').
    Cell 17 (code): result = predict_match('Flamengo', 'Palmeiras') + print. Then try/except block calling predict_match('TimeVinventado', 'Palmeiras') to verify ValueError is raised and printed (same pattern as notebook_logistic.ipynb cell 17).

    Note: notebook_gradient_boost.ipynb has NO markdown documentation cells (title/intro or results interpretation) in this plan — those are added in Plan 02 (Wave 2).
  </action>
  <verify>
    <automated>cd /home/theo/AI/Projeto_2_AI && source .venv/bin/activate && pytest --nbmake notebook_gradient_boost.ipynb -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - notebook_gradient_boost.ipynb exists and is valid JSON
    - pytest --nbmake notebook_gradient_boost.ipynb -x exits 0 (notebook runs end-to-end without error)
    - Notebook contains exactly the string 'HistGradientBoostingClassifier' in a code cell
    - Notebook contains 'class_weight' and "'balanced'" in the model instantiation cell
    - Notebook contains 'learning_rate=0.05' in the model instantiation cell
    - Notebook contains 'max_iter=300' in the model instantiation cell
    - Notebook contains 'max_depth=5' in the model instantiation cell
    - Notebook contains 'TimeSeriesSplit(n_splits=5)' in the cross-validation cell
    - Notebook output for classification_report shows non-zero recall for all three classes (HomeWin, Draw, AwayWin)
    - Notebook output for baseline comparison shows naive_acc = 0.48xx and model accuracy and macro-F1
    - predict_match('Flamengo', 'Palmeiras') cell output shows -> HomeWin or -> AwayWin or -> Draw (any valid label, no exception)
    - ValueError raised cell output contains "Unknown home_team 'TimeVinventado'"
    - Notebook contains no KFold or StratifiedKFold
    - Notebook contains no GridSearchCV or RandomizedSearchCV
  </acceptance_criteria>
  <done>notebook_gradient_boost.ipynb exists, runs end-to-end via nbmake, trains HistGradientBoostingClassifier(learning_rate=0.05, max_iter=300, max_depth=5, class_weight='balanced', random_state=42), outputs classification_report with non-zero recall for all three classes, and predict_match('Flamengo', 'Palmeiras') returns a valid W/D/L label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → predict_match | Team name strings from caller; untrusted free-text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04A-01 | Tampering | predict_match team name input | mitigate | ValueError raised for unknown team names; difflib suggestions guide valid input; VALID_TEAMS whitelist enforced |
| T-04A-02 | Information Disclosure | parquet file paths | accept | Local academic project; no PII; paths are relative to notebook directory |
| T-04A-SC | Tampering | No new package installs in this plan | accept | HistGradientBoostingClassifier is part of scikit-learn already in pyproject.toml; no new install required |
</threat_model>

<verification>
Run end-to-end notebook test:
  cd /home/theo/AI/Projeto_2_AI && source .venv/bin/activate && pytest --nbmake notebook_gradient_boost.ipynb -x -v

Spot-check output: classification_report shows non-zero Draw recall, baseline comparison shows 0.48xx naive accuracy, predict_match demo cell prints a valid result.
</verification>

<success_criteria>
notebook_gradient_boost.ipynb runs end-to-end without error, contains HistGradientBoostingClassifier(learning_rate=0.05, max_iter=300, max_depth=5, class_weight='balanced', random_state=42), reports classification metrics for all three classes, compares against the 49.6% naive baseline, and exposes predict_match(home_team, away_team) using the same lookup pattern as the Phase 3 notebooks.
</success_criteria>

<output>
Create `.planning/phases/04-advanced-model-notebook-polish/04-01-SUMMARY.md` when done.
</output>
