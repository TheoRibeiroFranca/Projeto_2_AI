---
phase: 02-feature-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebook_data.ipynb
  - dados/feature_matrix_train.parquet
  - dados/feature_matrix_test.parquet
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04

must_haves:
  truths:
    - "`uv sync` exits 0 and `python -c 'import sklearn'` succeeds inside .venv (Phase 3 prerequisite verified now)"
    - "`notebook_data.ipynb` contains a top-level function `def build_features(df):` defined in a single new code cell"
    - "Running `notebook_data.ipynb` end-to-end produces `dados/feature_matrix_train.parquet` (8025 rows, 31 columns) and `dados/feature_matrix_test.parquet` (1140 rows, 31 columns)"
    - "Both feature matrix parquets carry 11 base columns plus the 20 feature columns listed in RESEARCH.md Output Schema, in that exact name spelling and order"
    - "Feature columns have zero NaN in both feature_matrix_train.parquet and feature_matrix_test.parquet after D-06/D-07 imputation"
    - "Phase 1 parquets `dados/matches_train.parquet` (8025 rows) and `dados/matches_test.parquet` (1140 rows) remain byte-for-byte unchanged after Phase 2 cells execute"
    - "`pytest --nbmake notebook_data.ipynb -x` exits 0 with the new cells in place"
  artifacts:
    - path: "notebook_data.ipynb"
      provides: "Phase 1 data pipeline plus Phase 2 feature engineering cells (build_features function + apply/split/save)"
      contains: "def build_features"
      min_lines: 80
    - path: "dados/feature_matrix_train.parquet"
      provides: "Training feature matrix — 8025 rows (seasons 2003-2022), 31 columns (11 base + 20 features), zero NaN"
    - path: "dados/feature_matrix_test.parquet"
      provides: "Held-out feature matrix — 1140 rows (seasons 2023-2025), 31 columns (11 base + 20 features), zero NaN"
  key_links:
    - from: "notebook_data.ipynb"
      to: "dados/matches_train.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet\\(['\"]dados/matches_train\\.parquet"
    - from: "notebook_data.ipynb"
      to: "dados/matches_test.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet\\(['\"]dados/matches_test\\.parquet"
    - from: "notebook_data.ipynb (build_features)"
      to: "dados/feature_matrix_train.parquet"
      via: "DataFrame.to_parquet(engine='pyarrow')"
      pattern: "to_parquet\\(['\"]dados/feature_matrix_train\\.parquet"
    - from: "notebook_data.ipynb (build_features)"
      to: "dados/feature_matrix_test.parquet"
      via: "DataFrame.to_parquet(engine='pyarrow')"
      pattern: "to_parquet\\(['\"]dados/feature_matrix_test\\.parquet"
    - from: "build_features rolling cells"
      to: "season-scoped anti-leakage shift"
      via: "groupby(['team','season']).transform(lambda x: x.shift(1).rolling(5, min_periods=5))"
      pattern: "shift\\(1\\)\\.rolling\\(5, *min_periods=5"
---

<objective>
Extend `notebook_data.ipynb` with the Phase 2 feature engineering pipeline: define the canonical `build_features(df)` function, apply it to the combined train+test data, re-split by season, and save two new parquet files (`dados/feature_matrix_train.parquet`, `dados/feature_matrix_test.parquet`). Phase 1 parquets are NOT overwritten. This plan delivers all four Phase 2 feature requirements (FEAT-01 through FEAT-04) inside a single reusable function per locked decision D-03.

Purpose: Establish the Phase 2 -> Phase 3/4 contract — a 31-column feature matrix with zero NaN that every model notebook (logistic, random forest, gradient boost) loads directly via `pd.read_parquet`. The `build_features(df)` function is reusable so the matrix can be reconstructed from any filtered date range without modification (ROADMAP Phase 2 success criterion 5).

Output: Updated `notebook_data.ipynb` (cells 19 markdown + 20 code added — section "## 10. Feature Engineering") and two new parquet files in `dados/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-feature-engineering/02-CONTEXT.md
@.planning/phases/02-feature-engineering/02-RESEARCH.md
@.planning/phases/02-feature-engineering/02-PATTERNS.md
@.planning/phases/02-feature-engineering/02-VALIDATION.md
@.planning/phases/01-data-ingestion-cleaning/01-CONTEXT.md
@.planning/phases/01-data-ingestion-cleaning/01-01-SUMMARY.md
@CLAUDE.md
@pyproject.toml

<interfaces>
<!-- Phase 1 output contract (already on disk — DO NOT modify). -->
<!-- File: dados/matches_train.parquet (8025 rows × 11 columns) -->
<!-- File: dados/matches_test.parquet  (1140 rows × 11 columns) -->

Phase 1 parquet schema (input contract — read but never overwrite):
  Columns (in this exact order):
    ID            int64
    date          datetime64[ns]
    season        int (year extracted from date)
    round         int64
    home_team     object (string)
    away_team     object (string)
    home_score    int64
    away_score    int64
    home_state    object (string)
    away_state    object (string)
    result        object ('HomeWin' | 'Draw' | 'AwayWin' — exact strings, no other values)

  Verified counts:
    train: 8025 rows, seasons 2003-2022
    test:  1140 rows, seasons 2023-2025
    combined: 9165 rows total

Phase 2 output contract (Phase 3/4 will read these):
  Same 11 base columns above, plus 20 feature columns appended in this order
  (column name spelling MUST match exactly — Phase 3/4 notebooks will index by name):

    FEAT-01 (rolling 5-match mean):
      home_goals_scored_last5      float64
      home_goals_conceded_last5    float64
      away_goals_scored_last5      float64
      away_goals_conceded_last5    float64

    FEAT-02 (rolling 5-match sum, range 0-5):
      home_wins_last5              float64
      home_draws_last5             float64
      home_losses_last5            float64
      away_wins_last5              float64
      away_draws_last5             float64
      away_losses_last5            float64

    FEAT-03 (cumulative season % per venue, range 0-1):
      home_win_pct_season          float64
      home_draw_pct_season         float64
      home_loss_pct_season         float64
      away_win_pct_season          float64
      away_draw_pct_season         float64
      away_loss_pct_season         float64

    FEAT-04 (derived from FEAT-01/02 — zero additional data cost):
      home_goal_diff_last5         float64    (home_goals_scored_last5 - home_goals_conceded_last5)
      home_points_last5            float64    (home_wins_last5 * 3 + home_draws_last5; range 0-15)
      away_goal_diff_last5         float64
      away_points_last5            float64

  Final schema: 31 columns total (11 base + 20 features), 0 NaN anywhere in the feature columns.

build_features(df) signature contract (locked by D-03):
  def build_features(df: pd.DataFrame) -> pd.DataFrame
    Input:  DataFrame matching the Phase 1 parquet schema above (any filtered date range allowed).
    Output: DataFrame with len(input) rows and 31 columns in the exact order above.
            Zero NaN in feature columns after D-06/D-07 imputation.
    Side effects: none — pure function.

Imputation policy (locked by D-06/D-07):
  - D-06: For early-season NaN in FEAT-01/02 columns, fill with the mean of that
          team's last 10 matches from the PREVIOUS season. Scale by 5.0 for sum
          columns (win/drw/lss last5) so the imputed value matches the [0-5] range
          of the rolling sum.
  - D-07: For teams with no previous season data (first appearance or promoted team
          with no CSV history), fall back to 0.0.
  - FEAT-03 (cumulative % features): first match of season for a team gets 0.0
          fill (no NaN allowed in output); CONTEXT.md does not prescribe previous-
          season home rate for this case and 0.0 is the conservative default
          (flagged in RESEARCH.md Open Question 1).
</interfaces>

<patterns>
<!-- Established repo conventions — extracted from notebook_data.ipynb Cells 02 and 14-16. -->
<!-- These conventions are NOT negotiable: they keep the new cells stylistically aligned. -->

Convention 1 — No new imports at the top of Phase 2 cells.
  `pandas as pd`, `pyarrow`, `os`, `matplotlib.pyplot as plt`, `seaborn as sns`
  are already imported in Cell 02. Do NOT re-import them in Phase 2 cells. If a
  NaN sentinel is needed, use `float('nan')` — `numpy` is not imported in the
  notebook and Phase 2 should not introduce it.

Convention 2 — Section markdown header before each code cell.
  Insert a markdown cell `## 10. Engenharia de features` (or equivalent Portuguese
  title) BEFORE the `build_features` code cell. Phase 1 ended at section "## 9.
  Análise exploratória" (Cell 17 in the notebook); Phase 2 sections continue at 10.

Convention 3 — Anti-leakage rolling pattern (MANDATORY for all FEAT-01/02 columns):
    long.groupby(['team', 'season'])[col]
        .transform(lambda x: x.shift(1).rolling(5, min_periods=5).mean())
  - `.shift(1)` MUST come BEFORE `.rolling(5)` — never after.
  - `min_periods=5` (strict) — required so the leakage assertion in Plan 02 still works.
  - `.mean()` for goals_scored / goals_conceded; `.sum()` for win / drw / lss.

Convention 4 — `include_groups=False` on every `.groupby(...).apply(lambda ...)` call.
  pandas 2.3.3 raises FutureWarning without it and will error in 3.x. The
  imputation prior uses `.apply(lambda x: x.tail(10).mean(), include_groups=False)`.

Convention 5 — Assert-then-print after every transform.
  Each code cell ends with at least one `assert` followed by a `print(...)`.
  Assertion messages stay in Portuguese for shape/count checks:
    assert fm_train.shape == (8025, 31), f"Esperado (8025, 31), obtido {fm_train.shape}"
  English is acceptable inside `build_features` for technical-debug messages.

Convention 6 — Parquet save pattern (mirrors Cell 16):
    os.makedirs('dados', exist_ok=True)
    fm_train.to_parquet('dados/feature_matrix_train.parquet', engine='pyarrow', index=False)
    fm_test.to_parquet('dados/feature_matrix_test.parquet',   engine='pyarrow', index=False)
    assert os.path.exists('dados/feature_matrix_train.parquet')
    assert os.path.exists('dados/feature_matrix_test.parquet')
    reload_train = pd.read_parquet('dados/feature_matrix_train.parquet')
    assert reload_train.shape == (8025, 31), f"Esperado (8025, 31), obtido {reload_train.shape}"
    assert list(reload_train.columns) == expected_cols, ...

Convention 7 — Call `build_features` on the COMBINED train+test DataFrame, then re-split.
  Required by RESEARCH.md Pattern 5 / Pitfall 5. Splitting BEFORE build_features
  causes early-2023 NaN imputation to fall back to 0.0 for every test row whose
  team played in 2022 — that is a silent feature-quality bug, not a correctness
  bug, so it would never be caught by an assertion.

Convention 8 — Re-split by `season` column, not `date.dt.year`.
  Phase 1 used `date.dt.year`; Phase 2 should use the `season` column directly
  since it is the canonical groupby key already used for rolling windows
  (PATTERNS.md Shared Patterns -> "Temporal split key").

Convention 9 — Headless plotting (only relevant for Plan 02 stats cell — NOT this plan).
  This plan adds no plots. Stats output in Task 2 print summary uses plain
  `print(...)` — no matplotlib calls.

Convention 10 — Phase 1 parquets are read-only.
  Phase 2 cells may call `pd.read_parquet('dados/matches_train.parquet')` but
  must NOT call `.to_parquet('dados/matches_train.parquet'...)` or
  `os.remove('dados/matches_train.parquet')`. Phase 1 outputs are frozen.
</patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run uv sync to install scikit-learn (Phase 3 prerequisite, verified now)</name>
  <files>uv.lock</files>
  <read_first>
    - pyproject.toml (confirm `scikit-learn>=1.3` is already in the `[project] dependencies` list — no edit needed; only `uv sync` resolves it into `.venv`)
    - .planning/phases/02-feature-engineering/02-RESEARCH.md (section "Environment Availability" — confirms scikit-learn is in pyproject.toml but not in `.venv`; section "Open Questions" item 2 — confirms `uv sync` is the fix)
    - .planning/phases/02-feature-engineering/02-VALIDATION.md (Wave 0 Requirements — calls out this exact step)
  </read_first>
  <action>
    Sync the project environment so scikit-learn 1.7.x is installed in `.venv`. Phase 2 itself does not need sklearn, but Phase 3 will — and RESEARCH.md flagged that the venv is currently missing it despite pyproject.toml declaring it. Catching this now (in Wave 0 of Phase 2) avoids a surprise import error in Phase 3.

    Steps:
    1. From the repo root, run `uv sync`. This must complete without errors.
    2. Verify scikit-learn is importable from `.venv`: `python -c "import sklearn; print(sklearn.__version__)"`. The version printed must satisfy `>= 1.3` (RESEARCH.md expects 1.7.2 — accept anything `1.3.x` and newer).
    3. Do NOT modify `pyproject.toml` — the dependency is already declared. Do NOT add new packages. Do NOT touch `notebook_data.ipynb` in this task.

    If `uv sync` reports a lockfile mismatch (e.g., needs `--upgrade`), STOP and report — do not run `uv sync --upgrade` without confirmation, because that would also upgrade pandas/pyarrow and could change downstream parquet behavior.
  </action>
  <verify>
    <automated>uv sync && python -c "import sklearn, sys; v=tuple(int(p) for p in sklearn.__version__.split('.')[:2]); assert v >= (1,3), f'sklearn too old: {sklearn.__version__}'; print('sklearn', sklearn.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync` exits with code 0 from the repo root.
    - `python -c "import sklearn"` inside `.venv` exits 0.
    - `python -c "import sklearn; print(sklearn.__version__)"` prints a version string whose first two parts satisfy `>= 1.3` (parse as `tuple(int(p) for p in v.split('.')[:2]) >= (1,3)`).
    - `pyproject.toml` is unchanged after this task (no diff in the `[project] dependencies` block).
    - `notebook_data.ipynb` is unchanged after this task.
    - `python -c "import pandas, pyarrow; print(pandas.__version__, pyarrow.__version__)"` still succeeds (pandas and pyarrow remain importable post-sync).
  </acceptance_criteria>
  <done>
    The `.venv` now contains a working scikit-learn install satisfying the project's `>= 1.3` constraint. Phase 3 model notebooks will be able to `from sklearn.linear_model import LogisticRegression` without first running `uv sync` themselves. Phase 2 feature engineering (Task 2) can now proceed without environment risk.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add build_features(df) definition and apply/split/save cells to notebook_data.ipynb</name>
  <files>notebook_data.ipynb, dados/feature_matrix_train.parquet, dados/feature_matrix_test.parquet</files>
  <read_first>
    - notebook_data.ipynb (read the full notebook end-to-end — confirm Cell 02 imports include `os, pandas as pd, matplotlib.pyplot as plt, seaborn as sns, pyarrow`; confirm Cell 17 is the last existing cell ("## 9. Análise exploratória"); locate the exact JSON cell index AFTER which Phase 2 cells will be appended)
    - .planning/phases/02-feature-engineering/02-CONTEXT.md (locked decisions D-01 through D-07 — all seven are non-negotiable)
    - .planning/phases/02-feature-engineering/02-RESEARCH.md (section "Code Examples > Complete build_features skeleton" — verified prototype, copy the algorithm exactly; section "Common Pitfalls" — items 1-5 must each be avoided; section "Architecture Patterns > Pattern 1-5" — required idioms)
    - .planning/phases/02-feature-engineering/02-PATTERNS.md (sections "Pattern: Function definition cell style", "Pattern: Parquet save + round-trip assertion", "Pattern: Temporal split", "Shared Patterns")
    - .planning/phases/01-data-ingestion-cleaning/01-01-PLAN.md (confirms Phase 1 column names and parquet contract — the `Interfaces` block in this plan duplicates the schema but the executor should cross-check against the actual Phase 1 PLAN)
    - dados/matches_train.parquet (use `pd.read_parquet(...).columns.tolist()` and `pd.read_parquet(...).shape` to confirm 8025 rows × 11 columns and column order matches the `<interfaces>` block above)
    - dados/matches_test.parquet (same confirmation: 1140 rows × 11 columns)
    - CLAUDE.md ("Football Predictor — Critical Gotchas" — temporal leakage, NEVER use dropna() on feature matrix, draw class collapse, .shift(1) before .rolling())
  </read_first>
  <action>
    Append two markdown cells and two code cells to `notebook_data.ipynb`, placed AFTER the existing Cell 17 ("## 9. Análise exploratória" code cell). Do NOT remove or modify any existing Phase 1 cell. Use `jupyter nbconvert --to notebook --execute notebook_data.ipynb --inplace --ExecutePreprocessor.timeout=300` to materialize the executed outputs once the cells are in place.

    Insert in this exact order (cell indices given assuming the existing notebook ends at index 18; adjust if Phase 1's verify-fix added cells):

    Cell A (markdown) — section header for the function definition:
      `## 10. Engenharia de features`
      Followed by a 2-3 line Portuguese paragraph explaining that the section adds
      pre-match rolling and cumulative features via the reusable `build_features`
      function, uses `.shift(1).rolling(5)` to prevent leakage, and saves two new
      parquet files at `dados/feature_matrix_*.parquet` without overwriting the
      Phase 1 outputs.

    Cell B (code) — `build_features(df)` function definition. The function MUST
    implement the algorithm in RESEARCH.md "Complete build_features skeleton"
    exactly. The cell defines the function and nothing else — no call site, no
    file IO. Required structural elements (executor should follow the RESEARCH
    code excerpt, but every one of the following items must appear in the final
    function body):

      Step 1 — long-format team-match table:
        Build a `home` DataFrame from df with columns id, date, season, round,
        home_team -> team, home_score -> goals_scored, away_score -> goals_conceded,
        result. Add binary `win`/`drw`/`lss` columns derived from `result`
        (HomeWin -> win=1, Draw -> drw=1, AwayWin -> lss=1).
        Build an `away` DataFrame symmetrically (HomeWin from a home team is a
        loss for the away team). Concat into `long`, sort by ['team','season',
        'date','id'], reset index.

      Step 2 — season-scoped rolling features (FEAT-01, FEAT-02 raw):
        For `goals_scored` and `goals_conceded`: groupby(['team','season']).transform
        with `lambda x: x.shift(1).rolling(5, min_periods=5).mean()`. Output
        columns: `goals_scored_last5`, `goals_conceded_last5`.
        For `win`, `drw`, `lss`: same groupby pattern with `.sum()` instead of
        `.mean()`. Output columns: `win_last5`, `drw_last5`, `lss_last5`
        (range 0-5).
        `.shift(1)` MUST come BEFORE `.rolling(5)` and `min_periods=5` MUST be
        strict — this is the leakage guard Plan 02 will assert against.

      Step 3 — NaN imputation (D-06 / D-07):
        Build a helper that, for each source column, computes the team's previous-
        season last-10-match mean via
        `long.groupby(['team','season'])[src_col].apply(lambda x: x.tail(10).mean(), include_groups=False)`,
        adds 1 to the season to get the "next-season prior" key, and merges back.
        Apply with scale=1.0 for the mean features (goals_scored, goals_conceded)
        and scale=5.0 for the sum features (win, drw, lss — multiplying a [0,1]
        rate by the rolling window size 5 gives the [0,5] range of the rolling
        sum). Fill NaN with the prior, then fill any remaining NaN with 0.0 (D-07
        no-history fallback). `include_groups=False` is REQUIRED on every
        `.apply(lambda ...)` call inside this function — pandas 2.3.3 will warn
        without it and pandas 3.x will error.

      Step 4 — FEAT-04 derivation (after imputation so NaN does not propagate):
        `long['points_last5']    = long['win_last5'] * 3 + long['drw_last5']`
        `long['goal_diff_last5'] = long['goals_scored_last5'] - long['goals_conceded_last5']`

      Step 5 — FEAT-03 cumulative season % (home-venue and away-venue separately):
        Build `home_m = df[['id','date','season','home_team','result']]` sorted
        by ['home_team','season','date','id']. For each result label in
        [('hw','HomeWin'), ('hd','Draw'), ('hl','AwayWin')]: add a 0/1 column
        and a cumulative column via groupby(['home_team','season']).transform
        with `lambda x: x.shift(1).expanding().sum()`. Compute a parallel
        `h_n` count column via `lambda x: x.shift(1).expanding().count()`.
        Then divide cum / h_n.replace(0, float('nan')) to get the percentages,
        and `.fillna(0.0)` for the first-match-of-season rows. Output columns:
        `home_win_pct_season`, `home_draw_pct_season`, `home_loss_pct_season`.
        Build `away_m` symmetrically (label list flipped: AwayWin is the away
        team's win) with output columns `away_win_pct_season`,
        `away_draw_pct_season`, `away_loss_pct_season`.

      Step 6 — Pivot long-format back to match-level and merge home + away + pct:
        Build `match_home = df[['id','home_team']].rename(columns={'home_team':'team'})`
        and `match_away = df[['id','away_team']].rename(columns={'away_team':'team'})`.
        Inner-merge `long` with each on ['id','team'], select the 7 rolling
        feature columns (goals_scored_last5, goals_conceded_last5, win_last5,
        drw_last5, lss_last5, points_last5, goal_diff_last5) plus id, and
        rename to the `home_*` / `away_*` columns listed in `<interfaces>`.
        Note that `win_last5` -> `home_wins_last5` (plural), `drw_last5` ->
        `home_draws_last5`, `lss_last5` -> `home_losses_last5`. Then merge df
        with home_f, away_f, home_pct, away_pct on `id`.

      Final integrity assertions inside `build_features` (before return):
        `assert fm.shape[0] == len(df), f"Row count mismatch: {fm.shape[0]} != {len(df)}"`
        Build `feature_cols` as the 20 names defined in `<interfaces>`. Then:
        `assert fm[feature_cols].isna().sum().sum() == 0, "NaN found in feature matrix after imputation"`
        Return `fm`.

      The function MUST be a pure function — no print statements, no file IO,
      no global state. It returns a DataFrame.

    Cell C (code) — apply/split/save call site. This cell:

      1. Reads `dados/matches_train.parquet` and `dados/matches_test.parquet`
         from disk via `pd.read_parquet`. Do NOT reuse the in-memory `train_df`/
         `test_df` from earlier cells — reloading from parquet validates the
         Phase 1 contract round-trip.
      2. Concatenates with `pd.concat([train_df, test_df]).sort_values(['date','id']).reset_index(drop=True)`.
         Assert combined length == 9165.
      3. Calls `feature_matrix = build_features(combined)`.
         Assert `feature_matrix.shape == (9165, 31)`.
      4. Re-splits BY `season` (NOT by `date.dt.year` — per Convention 8):
         `fm_train = feature_matrix[feature_matrix['season'] <= 2022].copy()`
         `fm_test  = feature_matrix[feature_matrix['season'] >= 2023].copy()`
         Assert `len(fm_train) == 8025` and `len(fm_test) == 1140`.
         Assert `len(fm_train) + len(fm_test) == 9165`.
      5. Saves both parquets via the Phase 1 save pattern (Convention 6):
         `os.makedirs('dados', exist_ok=True)`
         `fm_train.to_parquet('dados/feature_matrix_train.parquet', engine='pyarrow', index=False)`
         `fm_test.to_parquet('dados/feature_matrix_test.parquet',   engine='pyarrow', index=False)`
         Assert both files exist via `os.path.exists`.
      6. Round-trip read-back: reload both parquets, assert
         `reload_train.shape == (8025, 31)` and `reload_test.shape == (1140, 31)`.
         Build the 20-name `expected_feature_cols` list inline (copy from
         `<interfaces>` exactly) and assert every name is in
         `reload_train.columns` AND in `reload_test.columns`. Assert
         `reload_train[expected_feature_cols].isna().sum().sum() == 0` AND
         `reload_test[expected_feature_cols].isna().sum().sum() == 0`.
      7. Final `print` summary (Portuguese, mirrors Cell 16 style):
         `print(f"Salvo: feature_matrix treino={len(fm_train)} linhas, teste={len(fm_test)} linhas, {len(expected_feature_cols)} features")`.

      Phase 1 parquets MUST NOT be written to in this cell (Convention 10). No
      `.to_parquet('dados/matches_train.parquet'...)` or `..._test.parquet` calls
      anywhere in the new cells.

    Cell D (markdown) — placeholder header for Plan 02 (do NOT add the leakage
    verification cell in this task; that is Plan 02's job). This markdown cell
    contains only the section title `## 11. Verificação de vazamento` and a
    one-line Portuguese note that the next cell (added in Plan 02) asserts
    pre-imputation round-1 NaN as the leakage guard. The placeholder lets the
    notebook structure read coherently after this task even before Plan 02
    runs. If preferred, Plan 02 can add this markdown header itself — in which
    case omit Cell D here. Executor should choose ONE: either add the
    placeholder now and leave the matching code cell to Plan 02, or omit the
    markdown header entirely and let Plan 02 add both. Document the choice in
    the SUMMARY.

    After all cells are inserted, execute the notebook end-to-end:
    `jupyter nbconvert --to notebook --execute notebook_data.ipynb --inplace --ExecutePreprocessor.timeout=300`.
    If execution fails on any cell, fix the cell — do not weaken assertions, do
    not call `dropna()`, do not introduce `numpy`. Then run the full nbmake
    validation: `pytest --nbmake notebook_data.ipynb -x` — it must exit 0.
  </action>
  <verify>
    <automated>jupyter nbconvert --to notebook --execute notebook_data.ipynb --inplace --ExecutePreprocessor.timeout=300 && pytest --nbmake notebook_data.ipynb -x && python -c "
import json, pandas as pd
nb = json.load(open('notebook_data.ipynb'))
src = '\n'.join(''.join(c.get('source', [])) for c in nb['cells'] if c.get('cell_type') == 'code')
assert 'def build_features' in src, 'build_features not defined'
assert 'shift(1).rolling(5' in src or 'shift(1)\n' in src and 'rolling(5' in src, 'shift/rolling anti-leakage pattern missing'
assert 'include_groups=False' in src, 'include_groups=False missing on apply'
assert 'feature_matrix_train.parquet' in src, 'train parquet save missing'
assert 'feature_matrix_test.parquet' in src, 'test parquet save missing'
# Verify Phase 1 parquets are NOT written from the new cells
nonblank = [l for l in src.split('\n') if l.strip() and not l.strip().startswith('#')]
nb_src = '\n'.join(nonblank)
assert \"to_parquet('dados/matches_train.parquet\" not in nb_src and 'to_parquet(\"dados/matches_train.parquet' not in nb_src, 'Phase 1 train parquet must not be overwritten'
assert \"to_parquet('dados/matches_test.parquet\" not in nb_src and 'to_parquet(\"dados/matches_test.parquet' not in nb_src, 'Phase 1 test parquet must not be overwritten'
# dropna ban
assert '.dropna(' not in nb_src, 'dropna() banned on feature matrix'
print('source checks OK')

fm_tr = pd.read_parquet('dados/feature_matrix_train.parquet')
fm_te = pd.read_parquet('dados/feature_matrix_test.parquet')
assert fm_tr.shape == (8025, 31), f'train shape {fm_tr.shape}'
assert fm_te.shape == (1140, 31), f'test shape  {fm_te.shape}'
expected = ['home_goals_scored_last5','home_goals_conceded_last5','away_goals_scored_last5','away_goals_conceded_last5','home_wins_last5','home_draws_last5','home_losses_last5','away_wins_last5','away_draws_last5','away_losses_last5','home_win_pct_season','home_draw_pct_season','home_loss_pct_season','away_win_pct_season','away_draw_pct_season','away_loss_pct_season','home_goal_diff_last5','home_points_last5','away_goal_diff_last5','away_points_last5']
for c in expected:
    assert c in fm_tr.columns, f'missing in train: {c}'
    assert c in fm_te.columns, f'missing in test:  {c}'
assert fm_tr[expected].isna().sum().sum() == 0, 'NaN in train features'
assert fm_te[expected].isna().sum().sum() == 0, 'NaN in test features'

# Phase 1 contract unchanged
m_tr = pd.read_parquet('dados/matches_train.parquet')
m_te = pd.read_parquet('dados/matches_test.parquet')
assert len(m_tr) == 8025 and len(m_te) == 1140, 'phase 1 row counts changed'
assert list(m_tr.columns) == ['ID','date','season','round','home_team','away_team','home_score','away_score','home_state','away_state','result'], 'phase 1 schema changed'
print('OK', fm_tr.shape, fm_te.shape)
"</automated>
  </verify>
  <acceptance_criteria>
    - `notebook_data.ipynb` contains a top-level `def build_features(df):` definition (grep-verifiable in cell source after `json.load`).
    - The notebook source contains the literal `shift(1).rolling(5` substring at least twice (FEAT-01 mean rolling and FEAT-02 sum rolling) AND the literal `min_periods=5` token.
    - The notebook source contains `include_groups=False` (required on every `.apply(lambda ...)`).
    - The notebook source does NOT contain `.dropna(` anywhere in non-comment lines.
    - The notebook source does NOT contain `to_parquet('dados/matches_train.parquet` or `to_parquet("dados/matches_train.parquet` (Phase 1 parquet read-only — Convention 10); same for `matches_test.parquet`.
    - `dados/feature_matrix_train.parquet` exists and `pd.read_parquet(...)` returns a DataFrame with shape `(8025, 31)`.
    - `dados/feature_matrix_test.parquet` exists and `pd.read_parquet(...)` returns a DataFrame with shape `(1140, 31)`.
    - Both feature matrix parquets contain ALL 20 feature column names listed in `<interfaces>`, with exact spelling: `home_goals_scored_last5, home_goals_conceded_last5, away_goals_scored_last5, away_goals_conceded_last5, home_wins_last5, home_draws_last5, home_losses_last5, away_wins_last5, away_draws_last5, away_losses_last5, home_win_pct_season, home_draw_pct_season, home_loss_pct_season, away_win_pct_season, away_draw_pct_season, away_loss_pct_season, home_goal_diff_last5, home_points_last5, away_goal_diff_last5, away_points_last5`.
    - Both feature matrix parquets have `0` NaN values across all 20 feature columns (`fm[feature_cols].isna().sum().sum() == 0`).
    - The 11 Phase 1 base columns (`ID, date, season, round, home_team, away_team, home_score, away_score, home_state, away_state, result`) are still present in both feature matrix parquets.
    - `dados/matches_train.parquet` still has 8025 rows × 11 columns with the Phase 1 column list unchanged (round-trip read after the new cells execute).
    - `dados/matches_test.parquet` still has 1140 rows × 11 columns with the Phase 1 column list unchanged.
    - `pytest --nbmake notebook_data.ipynb -x` exits 0 with the new cells in place.
    - FEAT-04 algebra holds within float tolerance on the train parquet: `(fm_train['home_points_last5'] - (fm_train['home_wins_last5']*3 + fm_train['home_draws_last5'])).abs().max() < 1e-9` AND `(fm_train['home_goal_diff_last5'] - (fm_train['home_goals_scored_last5'] - fm_train['home_goals_conceded_last5'])).abs().max() < 1e-9`. Same identities hold for the `away_*` counterparts.
    - The notebook contains a markdown cell whose source starts with `## 10. Engenharia de features` (or `## 10. Feature Engineering` — exact Portuguese spelling preferred; section number 10 is required).
  </acceptance_criteria>
  <done>
    `notebook_data.ipynb` now contains the `build_features(df)` function and the apply/split/save cell. Both `dados/feature_matrix_train.parquet` (8025 × 31) and `dados/feature_matrix_test.parquet` (1140 × 31) exist on disk with zero NaN in the 20 feature columns. Phase 1 parquets are untouched. `pytest --nbmake notebook_data.ipynb -x` is green. All four Phase 2 requirements (FEAT-01, FEAT-02, FEAT-03, FEAT-04) are implemented inside `build_features` per locked decision D-03. Plan 02 can now add the leakage verification and feature-stats cells on top of this output.
  </done>
</task>

</tasks>

<verification>
End-of-plan verification — all of the following must be true:

1. `uv sync` exits 0 and `python -c "import sklearn"` succeeds inside `.venv`.
2. `notebook_data.ipynb` defines `build_features(df)` as a top-level function in a single code cell (Cell B above).
3. The notebook source contains `shift(1).rolling(5` AND `min_periods=5` AND `include_groups=False`. None of these tokens is inside a comment.
4. The notebook source contains NO `.dropna(` call.
5. The notebook source does NOT call `.to_parquet(...)` on `dados/matches_train.parquet` or `dados/matches_test.parquet` (Phase 1 parquets are read-only after Phase 1).
6. `dados/feature_matrix_train.parquet` exists, has shape `(8025, 31)`, and contains all 20 feature columns with the exact spelling in `<interfaces>` and zero NaN in those columns.
7. `dados/feature_matrix_test.parquet` exists, has shape `(1140, 31)`, contains the same 20 feature columns and zero NaN in those columns.
8. Phase 1 parquets `dados/matches_train.parquet` (8025 × 11) and `dados/matches_test.parquet` (1140 × 11) remain readable and unchanged in schema.
9. FEAT-04 algebraic identities hold within `1e-9` on the train parquet:
   `home_points_last5 == 3 * home_wins_last5 + home_draws_last5`
   `home_goal_diff_last5 == home_goals_scored_last5 - home_goals_conceded_last5`
   (and the symmetric `away_*` identities).
10. `pytest --nbmake notebook_data.ipynb -x` exits 0.
</verification>

<success_criteria>
- FEAT-01 satisfied: `home_goals_scored_last5`, `home_goals_conceded_last5`, `away_goals_scored_last5`, `away_goals_conceded_last5` columns exist in both feature matrix parquets, computed via the `.shift(1).rolling(5, min_periods=5).mean()` season-scoped pattern inside `build_features`. (FEAT-01 acceptance — round-1 NaN — is asserted in Plan 02 against the raw pre-imputation series; this plan delivers the post-imputation columns.)
- FEAT-02 satisfied: `home_wins_last5`, `home_draws_last5`, `home_losses_last5`, `away_wins_last5`, `away_draws_last5`, `away_losses_last5` columns exist with values in `[0, 5]` (range may be float after imputation but stays in [0, 5] for non-imputed rows).
- FEAT-03 satisfied: `home_win_pct_season`, `home_draw_pct_season`, `home_loss_pct_season`, `away_win_pct_season`, `away_draw_pct_season`, `away_loss_pct_season` columns exist with values in `[0, 1]`. Per-team-per-current-season computation uses separate home-venue and away-venue paths per RESEARCH.md Pattern 2.
- FEAT-04 satisfied: `home_goal_diff_last5`, `home_points_last5`, `away_goal_diff_last5`, `away_points_last5` columns exist and the algebraic identities hold within `1e-9` (verification item 9 above).
- All locked CONTEXT.md decisions D-01 through D-07 are implemented:
  - D-01: Phase 2 cells appended to `notebook_data.ipynb` — no new notebook created.
  - D-02: `dados/feature_matrix_train.parquet` and `dados/feature_matrix_test.parquet` saved; Phase 1 parquets untouched.
  - D-03: `build_features(df)` function defined and used to produce both output files (single source of truth).
  - D-04: Rolling windows scoped via `groupby(['team','season'])` — windows reset at each season boundary.
  - D-05: FEAT-03 computed per team per current season via separate home-venue / away-venue paths.
  - D-06: Early-season NaN filled with previous-season last-10-match mean (scale=1.0 for mean features, scale=5.0 for sum features).
  - D-07: Teams with no previous season data fall back to 0.0.
- The deliverable is reproducible: `uv sync && pytest --nbmake notebook_data.ipynb -x` from a clean checkout regenerates both feature matrix parquets with the documented schema.
</success_criteria>

<output>
Create `.planning/phases/02-feature-engineering/02-01-SUMMARY.md` when done. The summary must record:
- Which files were created or modified (the three in `files_modified`, plus uv.lock if `uv sync` changed it).
- The cell indices in `notebook_data.ipynb` where the new markdown and code cells were inserted (e.g., "added cells at indices 19, 20, 21" — depending on whether Plan 01 added the Cell D markdown placeholder).
- Confirmation that Phase 1 parquets were NOT modified (record their pre- and post-task `os.path.getmtime` if helpful, or simply note "unchanged — verified by row-count + column-list assertion after task execution").
- The 20 feature column names and their final NaN counts (must all be 0).
- The post-imputation NaN rate before vs after imputation if measured (RESEARCH.md notes ~12.9% in long-format pre-imputation; final must be 0 in match-level columns).
- Confirmation that the `build_features` function is defined at top level of a single code cell, takes one `df` argument, and returns a DataFrame.
- The wall-clock runtime of `pytest --nbmake notebook_data.ipynb -x` (so 02-VALIDATION.md "Estimated runtime" can be cross-checked).
- Any deviation from the plan (there should be none; flag if there are).
- A note about the Cell D markdown placeholder decision (added now, or deferred to Plan 02).
</output>
