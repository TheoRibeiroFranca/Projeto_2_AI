---
phase: 03-baseline-model-notebooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebook_logistic.ipynb
autonomous: true
requirements:
  - MODEL-01
  - MODEL-04
  - EVAL-01
  - EVAL-02
  - EVAL-03

must_haves:
  truths:
    - "`notebook_logistic.ipynb` runs end-to-end without error via `pytest --nbmake notebook_logistic.ipynb -x`"
    - "LogisticRegression is instantiated with `class_weight='balanced'`, `solver='lbfgs'`, `max_iter=1000`, `random_state=42` — no other `fit()` call is present"
    - "`TimeSeriesSplit(n_splits=5)` is passed as the `cv` argument to `cross_val_score` — no `KFold` or `StratifiedKFold` appears anywhere in the notebook"
    - "`classification_report` is printed for `y_test` vs `y_pred` showing precision/recall/F1 for HomeWin, Draw, and AwayWin"
    - "A seaborn confusion matrix heatmap renders without error; all three classes (HomeWin, Draw, AwayWin) appear on both axes"
    - "Draw recall in the classification_report is non-zero (greater than 0.05)"
    - "A baseline comparison cell computes `(y_test == 'HomeWin').mean()` dynamically (no hardcoded 0.46 or 0.496) and prints both naive accuracy and model macro-F1"
    - "`predict_match('Flamengo', 'Palmeiras')` returns one of 'HomeWin', 'Draw', or 'AwayWin' without raising an exception"
    - "`predict_match` raises a `ValueError` containing the team name when an unknown team is passed"
  artifacts:
    - path: "notebook_logistic.ipynb"
      provides: "Self-contained Logistic Regression classifier notebook with CV, evaluation, baseline comparison, and predict_match function"
      contains: "LogisticRegression"
      min_lines: 80
  key_links:
    - from: "notebook_logistic.ipynb"
      to: "dados/feature_matrix_train.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet\\(['\"]dados/feature_matrix_train\\.parquet"
    - from: "notebook_logistic.ipynb"
      to: "dados/feature_matrix_test.parquet"
      via: "pd.read_parquet"
      pattern: "read_parquet\\(['\"]dados/feature_matrix_test\\.parquet"
    - from: "LogisticRegression.fit()"
      to: "predict_match lookup"
      via: "fitted lr object referenced inside predict_match()"
      pattern: "lr\\.predict\\("
---

<objective>
Create `notebook_logistic.ipynb` — a self-contained Logistic Regression baseline notebook that loads the Phase 2 feature matrices, trains a class-balanced classifier, reports TimeSeriesSplit cross-validation scores, evaluates on the held-out test parquet, visualizes the confusion matrix, compares against the naive baseline, and exposes a `predict_match(home_team, away_team)` function.

Purpose: Deliver MODEL-01, MODEL-04, EVAL-01, EVAL-02, EVAL-03 for the Logistic Regression baseline. This notebook proves that a simple linear classifier provides meaningful multi-class prediction (macro-F1 > naive ~0.33) even though raw accuracy may fall below the naive single-class 48.16% baseline — a deliberate tradeoff from `class_weight='balanced'`.

Output: `notebook_logistic.ipynb` — runnable end-to-end, passing `pytest --nbmake notebook_logistic.ipynb -x`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-baseline-model-notebooks/03-CONTEXT.md
@.planning/phases/03-baseline-model-notebooks/03-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notebook_logistic.ipynb — cells 1 through 15 (setup through baseline comparison)</name>

  <read_first>
    - `.planning/phases/03-baseline-model-notebooks/03-RESEARCH.md` — Pattern 1 (LogisticRegression with temporal CV), Pattern 4 (naive baseline comparison), full FEATURE_COLS list, anti-patterns section, empirical performance numbers, recommended cell structure (cells 1-15)
    - `.planning/phases/03-baseline-model-notebooks/03-CONTEXT.md` — D-02 (class_weight='balanced' mandatory), D-03 (TSS for CV score reporting only, no GridSearchCV), D-04 (never KFold/StratifiedKFold/shuffle=True), D-07 (classification_report per class), D-08 (seaborn heatmap, non-zero recall all classes), D-09 (compare against 49.6% baseline, compute dynamically), D-10 (section-divider markdown cells only)
    - `dados/feature_matrix_train.parquet` — verify it exists (do not re-derive features from it; just load with `pd.read_parquet`)
  </read_first>

  <action>
  Create `notebook_logistic.ipynb` as a new Jupyter notebook with the following cells in order. Use nbformat 4 / nbformat_minor 5. All markdown cells are section dividers only (D-10 — no prose explanations).

  Cell 1 (markdown): `## Setup`

  Cell 2 (code — setup and imports): Set `os.environ['MPLBACKEND'] = 'agg'` and `os.environ['MPLCONFIGDIR'] = '/tmp/matplotlib-config'` BEFORE any other import. Then import: `os`, `numpy as np`, `pandas as pd`, `seaborn as sns`, `matplotlib.pyplot as plt`, `from sklearn.linear_model import LogisticRegression`, `from sklearn.model_selection import TimeSeriesSplit, cross_val_score`, `from sklearn.metrics import classification_report, confusion_matrix, accuracy_score, f1_score`. Do NOT import KFold or StratifiedKFold.

  Cell 3 (markdown): `## Load Data`

  Cell 4 (code — load data): Load `train = pd.read_parquet('dados/feature_matrix_train.parquet')` and `test = pd.read_parquet('dados/feature_matrix_test.parquet')`. Print shapes of both.

  Cell 5 (markdown): `## Feature Selection`

  Cell 6 (code — feature selection): Define `NON_FEATURE_COLS` as the list of 11 non-feature column names: `['id', 'date', 'season', 'round', 'home_team', 'away_team', 'home_score', 'away_score', 'home_state', 'away_state', 'result']`. Define `FEATURE_COLS = [c for c in train.columns if c not in NON_FEATURE_COLS]`. Build `X_train = train[FEATURE_COLS]`, `y_train = train['result']`, `X_test = test[FEATURE_COLS]`, `y_test = test['result']`. Print `len(FEATURE_COLS)` to confirm 20 columns.

  Cell 7 (markdown): `## Model Training`

  Cell 8 (code — model training): Instantiate `lr = LogisticRegression(class_weight='balanced', solver='lbfgs', max_iter=1000, random_state=42)`. Call `lr.fit(X_train, y_train)`. Print `"Model trained."`.

  Cell 9 (markdown): `## Cross-Validation`

  Cell 10 (code — cross-validation): Create `tss = TimeSeriesSplit(n_splits=5)`. Run `cv_acc = cross_val_score(lr, X_train, y_train, cv=tss, scoring='accuracy')` and `cv_f1 = cross_val_score(lr, X_train, y_train, cv=tss, scoring='f1_macro')`. Print formatted strings showing mean +/- std for both metrics. Use variable names `cv_acc` and `cv_f1`.

  Cell 11 (markdown): `## Evaluation`

  Cell 12 (code — classification report): Set `y_pred = lr.predict(X_test)`. Print `classification_report(y_test, y_pred, target_names=['HomeWin', 'Draw', 'AwayWin'])`.

  Cell 13 (code — confusion matrix heatmap): Define `labels = ['HomeWin', 'Draw', 'AwayWin']`. Compute `cm = confusion_matrix(y_test, y_pred, labels=labels)`. Create a figure with `plt.figure(figsize=(6, 5))`. Call `sns.heatmap(cm, annot=True, fmt='d', xticklabels=labels, yticklabels=labels, cmap='Blues')`. Add `plt.xlabel('Predicted')`, `plt.ylabel('Actual')`, `plt.title('Logistic Regression — Confusion Matrix')`. Call `plt.tight_layout()` then `plt.show()`.

  Cell 14 (markdown): `## Baseline Comparison`

  Cell 15 (code — baseline comparison): Compute `naive_acc = (y_test == 'HomeWin').mean()` (dynamic, no hardcoded value). Compute `model_acc = accuracy_score(y_test, y_pred)`. Compute `model_f1 = f1_score(y_test, y_pred, average='macro')`. Print all three values with labels. Add a print statement: `"Note: class_weight=balanced trades raw accuracy for Draw/AwayWin recall. Primary metric is macro-F1."`.
  </action>

  <verify>
    <automated>cd /home/theo/AI/Projeto_2_AI && source .venv/bin/activate && python -c "import nbformat; nb = nbformat.read('notebook_logistic.ipynb', as_version=4); print(f'Cells: {len(nb.cells)}')"</automated>
  </verify>

  <acceptance_criteria>
  - `notebook_logistic.ipynb` exists at repo root
  - Notebook has at least 15 cells
  - Cell 2 (setup code) sets `os.environ['MPLBACKEND']` before any seaborn or matplotlib import
  - `LogisticRegression` appears in the notebook with `class_weight='balanced'`, `solver='lbfgs'`, `max_iter=1000`, `random_state=42` as constructor arguments
  - `TimeSeriesSplit(n_splits=5)` appears and is passed as `cv=tss` to `cross_val_score`
  - The strings `'KFold'`, `'StratifiedKFold'`, `'shuffle=True'` do NOT appear anywhere in the notebook source
  - `classification_report` is called with `y_test, y_pred`
  - `sns.heatmap` is called with `annot=True, fmt='d'`
  - `(y_test == 'HomeWin').mean()` appears (not `0.46` or `0.496` hardcoded)
  - `f1_score(y_test, y_pred, average='macro')` appears
  </acceptance_criteria>

  <done>Notebook file exists with all 15 cells implementing setup, data loading, feature selection, model training, TSS cross-validation, classification report, seaborn confusion matrix, and naive baseline comparison per D-02 through D-10.</done>
</task>

<task type="auto">
  <name>Task 2: Add predict_match cells (cells 16-18) and validate notebook end-to-end with nbmake</name>

  <read_first>
    - `.planning/phases/03-baseline-model-notebooks/03-RESEARCH.md` — Pattern 3 (predict_match lookup function), Pitfall 4 (column ordering mismatch — always reindex with `row[FEATURE_COLS]` after concat), Pitfall 5 (MPLBACKEND must be set before import), valid VALID_TEAMS list (46 team names), FEATURE_COLS, HOME_FEAT_COLS, AWAY_FEAT_COLS definitions
    - `.planning/phases/03-baseline-model-notebooks/03-CONTEXT.md` — D-05 (lookup-based predict_match using groupby on combined parquet), D-06 (ValueError with team name + suggestion when team not found)
    - `notebook_logistic.ipynb` — read current cell count and structure before appending
  </read_first>

  <action>
  Append three cells to `notebook_logistic.ipynb` (after cell 15). Edit the notebook JSON directly using nbformat or by re-writing the file:

  Cell 16 (markdown): `## predict_match`

  Cell 17 (code — lookup tables and predict_match function): Define `HOME_FEAT_COLS = [c for c in FEATURE_COLS if c.startswith('home_')]` and `AWAY_FEAT_COLS = [c for c in FEATURE_COLS if c.startswith('away_')]`. Build the combined lookup: `combined = pd.concat([train, test]).sort_values('date').reset_index(drop=True)`. Create `home_last = combined.groupby('home_team')[HOME_FEAT_COLS].last()` and `away_last = combined.groupby('away_team')[AWAY_FEAT_COLS].last()`. Define `VALID_TEAMS = sorted(combined['home_team'].unique().tolist())`.

  Define function `predict_match(home_team: str, away_team: str) -> str:`. Inside: check `if home_team not in home_last.index:` raise `ValueError(f"Unknown team '{home_team}'. Check spelling. Valid teams: {VALID_TEAMS}")`. Same guard for `away_team` against `away_last.index`. Then `home_row = home_last.loc[home_team]`, `away_row = away_last.loc[away_team]`, `row = pd.concat([home_row, away_row])[FEATURE_COLS]`, `vector = row.values.reshape(1, -1)`, `return lr.predict(vector)[0]`.

  Cell 18 (code — example call): `result = predict_match('Flamengo', 'Palmeiras')` then `print(f"predict_match('Flamengo', 'Palmeiras') -> {result}")`. Also demonstrate the ValueError: wrap `predict_match('TimeVinventado', 'Palmeiras')` in a try/except that prints the ValueError message.

  After writing the notebook, run `pytest --nbmake notebook_logistic.ipynb -x` from the repo root with the venv activated. If nbmake fails, read the error output and fix the failing cell. Common failure causes: column mismatch in predict_match (fix: ensure `row[FEATURE_COLS]` reindex step is present), MPLBACKEND not set before import (fix: move os.environ lines to top of Cell 2).
  </action>

  <verify>
    <automated>cd /home/theo/AI/Projeto_2_AI && source .venv/bin/activate && pytest --nbmake notebook_logistic.ipynb -x 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `pytest --nbmake notebook_logistic.ipynb -x` exits 0
  - `notebook_logistic.ipynb` contains a cell with `def predict_match(home_team`
  - `predict_match` function body contains `home_last.loc[home_team]` and `away_last.loc[away_team]`
  - `pd.concat([home_row, away_row])[FEATURE_COLS]` appears (ensuring column reindex, per Pitfall 4)
  - `ValueError` is raised in the function body when team is not found (contains `raise ValueError`)
  - `predict_match('Flamengo', 'Palmeiras')` example call is present and the notebook runs it without exception
  - `lr.predict(vector)` is called inside `predict_match` (fitted model is referenced, not re-fit)
  </acceptance_criteria>

  <done>`notebook_logistic.ipynb` is complete with 18 cells, all cells execute without error via nbmake, `predict_match` returns a valid W/D/L label for known teams and raises ValueError for unknown teams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parquet → notebook | Feature matrices loaded from local filesystem; no external network calls in this notebook |
| predict_match input | `home_team` and `away_team` are user-supplied strings; validated against known team index before model inference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | feature_matrix parquets | accept | Local academic project; parquets are read-only pipeline outputs; no integrity verification needed |
| T-03-02 | Information Disclosure | predict_match VALID_TEAMS in ValueError | accept | Exposing valid team names in error message is the intended UX per D-06; no sensitive data involved |
| T-03-03 | Elevation of Privilege | notebook cell execution order | mitigate | All globals (lr, FEATURE_COLS, home_last, away_last) defined in prior cells; predict_match cell must run after training cell — enforced by sequential nbmake execution |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; all packages verified [OK] in Package Legitimacy Audit (RESEARCH.md); scikit-learn 1.8.0, pandas 2.3.3, seaborn 0.13.2, pyarrow 24.0.0 already in venv |
</threat_model>

<verification>
Full phase verification:
- `pytest --nbmake notebook_logistic.ipynb -x` exits 0
- Notebook classification_report output shows non-zero recall for Draw class (> 0.05)
- Notebook prints naive_acc computed dynamically from `(y_test == 'HomeWin').mean()`
- Notebook prints model macro-F1 from `f1_score(y_test, y_pred, average='macro')`
- No `KFold`, `StratifiedKFold`, or `shuffle=True` string found in notebook source
- `predict_match('Flamengo', 'Palmeiras')` cell executes and returns one of: 'HomeWin', 'Draw', 'AwayWin'
</verification>

<success_criteria>
`notebook_logistic.ipynb` exists, passes `pytest --nbmake notebook_logistic.ipynb -x`, reports non-zero Draw recall via `classification_report`, compares against the dynamically-computed naive baseline (not hardcoded 46%), exposes `predict_match()` that returns W/D/L for known teams and raises ValueError for unknown teams. Macro-F1 is reported alongside raw accuracy. Section-divider markdown cells only (D-10).
</success_criteria>

<output>
Create `.planning/phases/03-baseline-model-notebooks/03-01-SUMMARY.md` when done.
</output>
